schema {
  query: Query
  mutation: Mutation
}

type Mutation {
  incrementCounter: Int
  message: String
}

interface Node {
  id: ID!
}

type Person implements Node {
  id: ID!
  name: String
  age: Int
}

type Query {
  store: Store
}

type Store {
  counter: Int
  person: Person
}
