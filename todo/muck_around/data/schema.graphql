schema {
  query: Query
  mutation: Mutation
}

type Mutation {
  incrementCounter: Int
  message: String
}

type Person {
  id: Int
  name: String
  age: Int
  debug: String
  friends: [Person]
}

type Query {
  store: Store
}

type Store {
  counter: Int
  person(id: Int): Person
}
