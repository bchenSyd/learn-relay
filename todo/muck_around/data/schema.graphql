schema {
  query: Query
  mutation: Mutation
}

type Mutation {
  incrementCounter: Int
  message: String
}

type Person {
  name: String
  age: Int
  debug: String
  friends: [Person]
}

type Query {
  store: Store
}

type Store {
  counter: Int
  person: Person
}
