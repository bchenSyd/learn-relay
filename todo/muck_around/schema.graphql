schema {
  query: Store
  mutation: Mutation
}

type Mutation {
  incrementCounter: Int
  message: String
}

type Person {
  id: Int
  name: String
  age: Int
  debug: String
  friends: [Person]
}

type Store {
  counter: Int
  person(id: Int): Person
}
